# 🏗️ Application Architecture

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER BROWSER                             │
└─────────────────────────────────────────────────────────────────┘
                              ▼
        ┌──────────────────────────────────────────┐
        │      VITE DEV SERVER (localhost:5173)    │
        │         or GitHub Pages (prod)          │
        └──────────────────────────────────────────┘
                              ▼
        ┌──────────────────────────────────────────┐
        │          Vue 3 App (App.vue)             │
        └──────────────────────────────────────────┘
           │                │                │
           ▼                ▼                ▼
      ┌─────────┐    ┌─────────┐    ┌─────────────┐
      │ Router  │    │ Pinia   │    │  Components │
      │ (guards)│    │ Stores  │    │  & Pages    │
      └─────────┘    └─────────┘    └─────────────┘
           │              │ │            │
           │              │ │            │
      Routes          ┌───┘ └────┐       │
      Protected       │          │       │
      /login      ┌───▼─┐    ┌──▼──┐    │
      /products   │Auth │    │Prod │    │
      /products/  │Store│    │Store│    │
      /products/  └─┬───┘    └──┬──┘    │
      /new           │          │       │
                     └──────┬───┘       │
                            ▼           │
                    ┌──────────────┐    │
                    │ localStorage │    │
                    └──────────────┘    │
                                        │
      Pinia ←─────────────────────────┘
      Composition API
      (reactive)
                            ▼
        ┌──────────────────────────────────────┐
        │         Pages (4 Vue Components)     │
        │  ┌──────────┐  ┌──────────────────┐ │
        │  │ Login    │  │ Products (table) │ │
        │  └──────────┘  └──────────────────┘ │
        │  ┌──────────────┐  ┌────────────┐   │
        │  │ ProductView  │  │ AddProduct │   │
        │  └──────────────┘  └────────────┘   │
        └──────────────────────────────────────┘
                            │
                            ▼
        ┌──────────────────────────────────────┐
        │         Tailwind CSS Styling         │
        │  (Responsive, Primary: #000080)      │
        └──────────────────────────────────────┘
                            │
                            ▼
        ┌──────────────────────────────────────┐
        │           API Layer (Axios)          │
        └──────────────────────────────────────┘
                            │
                            ▼
        ┌──────────────────────────────────────┐
        │        DummyJSON API                 │
        │  • POST /auth/login                  │
        │  • GET /products                     │
        │  • GET /products/:id                 │
        │  • POST /products/add                │
        │  • DELETE /products/:id              │
        └──────────────────────────────────────┘
```

---

## Data Flow Diagram

```
LOGIN FLOW:
┌─────────┐     credentials     ┌────────┐     HTTP      ┌──────────┐
│ Login   │──────────────────→  │ Axios  │───────────→  │ DummyJSON│
│ Component                    │ POST   │     /auth     │   API    │
└─────────┘                    │ /login │     login     └──────────┘
    ▲                          └───┬────┘                   │
    │                              │ token                 │
    │                              ▼                        │
    └─────────────────────────┌─────────┐◄──────────────────┘
                              │Auth     │
                              │Store    │
                              │(Pinia)  │
                              └────┬────┘
                                   │
                    ┌──────────────┴────────────────┐
                    ▼                               ▼
            ┌──────────────┐              ┌─────────────────┐
            │ localStorage │              │ Vue Components  │
            │ (session)    │              │ (reactive)      │
            └──────────────┘              └─────────────────┘


PRODUCT FETCH FLOW:
┌────────────────┐     request + token     ┌────────┐     HTTP      ┌──────────┐
│ Products       │───────────────────────→  │ Axios  │───────────→  │ DummyJSON│
│ Component      │                          │ GET    │  /products   │   API    │
└────────────────┘                          │        │  (limit:100) └──────────┘
       ▲                                    └───┬────┘                  │
       │                                        │ [products]           │
       │                                        ▼                       │
       │                                  ┌─────────────┐◄──────────────┘
       │                                  │ Product     │
       │                                  │ Store       │
       │                                  │ (Pinia)     │
       │                                  └──────┬──────┘
       │                                         │
       └─────────────────────────────────────────┘
              (reactive update)


ADD PRODUCT FLOW:
┌───────────────┐     form data     ┌────────┐     HTTP      ┌──────────┐
│ AddProduct    │──────────────────→ │ Axios  │───────────→  │ DummyJSON│
│ Form          │                    │ POST   │ /products/   │   API    │
└───────────────┘                    │ /add   │    add       └──────────┘
       ▲                             └───┬────┘                  │
       │                                 │ [new product]        │
       │                                 ▼                       │
       │                          ┌────────────────┐◄────────────┘
       │                          │ Product        │
       │                          │ Store (update) │
       │                          └────────┬───────┘
       │                                   │
       └───────────────────────────────────┘
         (redirect to ProductView)
```

---

## Component Tree

```
App.vue (root)
│
├── RouterView
│   │
│   ├── Login.vue
│   │   ├── Form Input (username, password)
│   │   ├── useAuthStore()
│   │   │   └── login(credentials)
│   │   │       ├── POST /auth/login
│   │   │       ├── Store token
│   │   │       └── localStorage persist
│   │   └── Route guard check
│   │
│   ├── Products.vue
│   │   ├── Header
│   │   │   ├── Title
│   │   │   └── Logout button
│   │   ├── Search Bar
│   │   │   ├── Input (title)
│   │   │   └── Refresh button
│   │   ├── Add Product Button
│   │   └── Products Table
│   │       ├── Thead (headers)
│   │       ├── Tbody
│   │       │   └── ProductRow (×N)
│   │       │       ├── Thumbnail
│   │       │       ├── Title + Description
│   │       │       ├── Category
│   │       │       ├── Price
│   │       │       ├── Stock (colored badge)
│   │       │       └── View button (→ ProductView)
│   │       └── useProductStore()
│   │           └── fetchProducts()
│   │
│   ├── ProductView.vue
│   │   ├── Back button
│   │   ├── Logout button
│   │   ├── Product Details
│   │   │   ├── Large image
│   │   │   ├── Title
│   │   │   ├── Description
│   │   │   ├── Grid (Category, Price, Stock, Rating)
│   │   │   └── Discount badge (conditional)
│   │   ├── Delete button
│   │   │   └── Confirmation dialog
│   │   │       └── DELETE /products/:id
│   │   └── useProductStore()
│   │       ├── fetchProductById(id)
│   │       └── deleteProduct(id)
│   │
│   └── AddProduct.vue
│       ├── Header with title
│       ├── Logout button
│       ├── Form
│       │   ├── Input: Title (required)
│       │   ├── Textarea: Description
│       │   ├── Input: Category
│       │   ├── Input: Price
│       │   ├── Input: Stock
│       │   ├── Input: Thumbnail URL
│       │   ├── Input: Discount %
│       │   ├── Submit button
│       │   └── Cancel button
│       ├── Validation
│       └── useProductStore()
│           └── addProduct(form)
│               └── POST /products/add
│
└── Pinia Stores (global state)
    │
    ├── Auth Store
    │   ├── State
    │   │   ├── token
    │   │   ├── user
    │   │   └── error
    │   ├── Getter
    │   │   └── isAuthenticated
    │   └── Actions
    │       ├── login(credentials)
    │       ├── logout()
    │       └── restoreSession()
    │
    └── Product Store
        ├── State
        │   ├── products[]
        │   ├── isLoading
        │   └── error
        └── Actions
            ├── fetchProducts()
            ├── fetchProductById(id)
            ├── addProduct(payload)
            └── deleteProduct(id)
```

---

## File Structure Tree

```
project-app/
│
├── .github/
│   └── workflows/
│       └── deploy.yml          # GitHub Actions auto-deploy
│
├── src/
│   ├── pages/                  # Page components (route views)
│   │   ├── Login.vue
│   │   ├── Products.vue
│   │   ├── ProductView.vue
│   │   └── AddProduct.vue
│   │
│   ├── stores/                 # Pinia stores (state mgmt)
│   │   ├── auth.js
│   │   └── products.js
│   │
│   ├── router/                 # Vue Router config
│   │   └── index.js
│   │
│   ├── components/             # Reusable components (empty)
│   │
│   ├── App.vue                 # Root component
│   ├── main.js                 # App entry
│   └── styles.css              # Tailwind imports
│
├── index.html                  # HTML entry point
├── package.json                # npm dependencies
├── vite.config.js              # Vite config
├── tailwind.config.cjs         # Tailwind theme
├── postcss.config.cjs          # PostCSS config
├── .gitignore                  # Git ignore
├── .npmrc                       # npm config
│
├── Documentation/
│   ├── README.md               # Main docs
│   ├── DEPLOYMENT.md           # GitHub Pages guide
│   ├── QUICK_START.md          # Setup guide
│   ├── SUBMISSION_SUMMARY.md   # Implementation
│   ├── PROJECT_COMPLETION.md   # Completion report
│   ├── INVENTORY.md            # File inventory
│   └── ARCHITECTURE.md         # This file
│
└── Setup/
    └── setup-windows.bat       # Windows auto-setup
```

---

## State Management Flow

```
User Action
    │
    ▼
Component Method
    │
    ▼
Pinia Store Action
    │
    ├─→ API Call (Axios)
    │   │
    │   ▼
    │ DummyJSON API
    │   │
    │   ▼
    │ Response
    │
    ▼
Update Store State
    │
    ▼
Component Reactivity
    │
    ▼
Re-render UI
```

---

## Authentication Flow

```
┌──────────────────────────────────────────┐
│        User lands on /login              │
└──────────────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────┐
│ Check localStorage for 'auth' key        │
└──────────────────────────────────────────┘
           │
      ┌────┴────┐
      │          │
   Found    Not Found
      │          │
      ▼          ▼
   Redirect  Show Login Form
   to        │
   /products │
      │      │ User enters credentials
      │      │
      │      ▼
      │   POST /auth/login
      │      │
      │      ├─→ Success: token + user
      │      │   │
      │      │   ▼
      │      │  Store in Pinia
      │      │   │
      │      │   ▼
      │      │  Save to localStorage
      │      │   │
      │      │   ▼
      │      │  Redirect to /products
      │      │
      │      └─→ Failure: show error
      │          │
      │          ▼
      │      User retries
      │
      └──────→ Route Guard Check
              │
              └─→ Token valid?
                  ├─→ Yes: Allow access
                  └─→ No: Redirect to /login
```

---

## Deployment Architecture

```
LOCAL DEVELOPMENT
┌─────────────────────────────────────────┐
│   npm run dev                           │
│   ↓                                     │
│   Vite Dev Server (localhost:5173)     │
│   ↓                                     │
│   Hot Module Replacement                │
│   ↓                                     │
│   Browser auto-reload                  │
└─────────────────────────────────────────┘


PRODUCTION BUILD
┌─────────────────────────────────────────┐
│   npm run build                         │
│   ↓                                     │
│   Vite Build Process                    │
│   ├─ Tree-shake unused code             │
│   ├─ Minify JS/CSS                      │
│   └─ Output to dist/                    │
│       └─ index.html                     │
│       └─ /assets/*.js                   │
│       └─ /assets/*.css                  │
│       └─ ...                            │
└─────────────────────────────────────────┘


GITHUB PAGES DEPLOYMENT
┌─────────────────────────────────────────┐
│   Option A: Push dist/ folder           │
│   ├─ Settings → Pages                   │
│   ├─ Branch: main, Folder: /dist        │
│   └─ Auto-deployed on push              │
│                                         │
│   Option B: GitHub Actions              │
│   ├─ .github/workflows/deploy.yml       │
│   ├─ Triggers on push to main           │
│   ├─ Runs npm build                     │
│   ├─ Deploys to gh-pages branch         │
│   └─ Auto-deployed on every push        │
│                                         │
│   Result:                               │
│   https://YOUR_USERNAME.github.io/      │
│   product-manager                       │
└─────────────────────────────────────────┘
```

---

## API Request/Response Flow

```
AUTHENTICATED REQUEST:

┌─────────────────────────────────────────────┐
│        Component Initiates Request          │
│   (e.g., Search, Add, Delete)               │
└─────────────────────────────────────────────┘
                    ▼
┌─────────────────────────────────────────────┐
│       Pinia Store Action Called              │
│   (e.g., deleteProduct(id))                 │
└─────────────────────────────────────────────┘
                    ▼
┌─────────────────────────────────────────────┐
│      Axios HTTP Request                     │
│  ├─ URL: https://dummyjson.com/...         │
│  ├─ Method: GET/POST/DELETE                │
│  ├─ Headers:                                │
│  │   ├─ Content-Type: application/json      │
│  │   └─ Authorization: Bearer {token}       │
│  └─ Body: {payload}                        │
└─────────────────────────────────────────────┘
                    ▼
┌─────────────────────────────────────────────┐
│      DummyJSON API Processing               │
│  ├─ Validate token                          │
│  ├─ Execute operation                       │
│  └─ Return response                         │
└─────────────────────────────────────────────┘
                    ▼
┌─────────────────────────────────────────────┐
│      Response Handler                       │
│  ├─ Success (200):                          │
│  │   ├─ Update Pinia state                  │
│  │   └─ Trigger UI re-render                │
│  └─ Error (4xx/5xx):                        │
│      ├─ Display error message               │
│      └─ Store error state                   │
└─────────────────────────────────────────────┘
                    ▼
┌─────────────────────────────────────────────┐
│      Component Reactivity Update            │
│  ├─ Show success/error                      │
│  ├─ Navigate if needed                      │
│  └─ Update loading state                    │
└─────────────────────────────────────────────┘
```

---

**Architecture Complete** ✅

This demonstrates a clean, modular, and scalable Vue 3 architecture that follows modern best practices.

